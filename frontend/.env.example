# Backend API base URL for production builds (set this in Vercel).
# Example: https://strategic-analyzer-api.onrender.com
# Leave empty for local dev — Vite proxies /api to http://localhost:3001.
VITE_API_BASE=
